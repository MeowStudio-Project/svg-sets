// Intentionally does nothing during Cloudflare deploy.
// ZIP archives are served from the data repo (see src/lib/dataBase.ts).
console.log('[info] Skip copy-archives: ZIPs are served from external data repo');
