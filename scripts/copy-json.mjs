// Intentionally does nothing during Cloudflare deploy.
// Icon JSON is loaded from the data repo (see src/lib/dataBase.ts).
console.log('[info] Skip copy-json: icons are served from external data repo');
